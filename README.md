# Hono-ToDo

ToDo app, using Hono framework and applying Domain Driven Design (DDD) on project architecture.


## Running

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000
